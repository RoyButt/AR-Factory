<div class="wrap">
  <%= render "shared/sidebar" %>
  <div class="main">
    <div class="top"><h1>Edit Cutwork Party — <%= @party.name %></h1><%= link_to "← All parties", cutwork_parties_path, class: "btn-soft" %></div>
    <%= render "form" %>
  </div>
</div>
