<!DOCTYPE html>
<html>
<head>
  <title>Handmade Pass</title>
  <meta charset="utf-8">
  <style>
    @page { size: A5; margin: 10mm; }
    * { box-sizing: border-box; }
    body { font-family: -apple-system, Segoe UI, Roboto, Arial, sans-serif; margin: 0; color: #1f2d3d; }
    .hp { width: 190mm; max-width: 100%; margin: 0 auto; padding: 14px 18px; }
    @media print { .hp-actions { display: none !important; } body { width: 210mm; } }
  </style>
</head>
<body><%= yield %></body>
</html>
